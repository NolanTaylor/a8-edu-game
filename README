View classes: MainWindow, SelectClient, StartMenu
Model classes: Client(single client information), Model(holds a map of clients + the state of the game)

Client:
    Holds an image/s, viability, payment, name, dialogue/story, and unique ID
Model:
    Holds a map of current clients and a map of every unused clients, money, and reputation

parse the client file as follows:
    name
    ID
    viability
    payment
    image path
    information dialogue
    end of information dialouge
    reject dialouge
    accept dialouge

--Delete SelectClient class, unecessary

Accept/Reject added

--Implement a proper reset
--[Model]Keep track of the current client, get them out of the picture once the round ends (cannot be selected again
